# Product images go here

Upload product photos to this folder. Use simple filenames — lowercase, no spaces,
ending in `.jpg` or `.png` (e.g. `blender.jpg`) — matching the `image` field for
that product in `products.js`.

Square photos (1:1) work best since product cards crop to a square.
